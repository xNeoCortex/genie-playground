import { Module } from '@nestjs/common';

@Module({
  imports: [
    // TODO: Add AuthModule, ProductsModule, OrdersModule, StripeModule, EmailModule
  ],
  controllers: [],
  providers: [],
})
export class AppModule {}